<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
    <style>
      body {
        font-family: 'Times New Roman', Times, serif;
      }
    </style>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto p-5 flex justify-center items-center min-h-screen">
      <div class="flex flex-col items-center">
        <img src="/images/pokesign.png" alt="Pole Sign" class="mb-5 w-120 h-32">
        <div class="bg-white shadow-lg rounded-lg overflow-hidden w-96 transform transition-transform hover:scale-105">
          <div class="bg-yellow-400 p-4 border-b-4 border-yellow-600">
            <h1 class="text-4xl font-bold text-center text-gray-800 capitalize">
              <%= pokemon.name %>
            </h1>
          </div>
          <div class="p-6">
            <div class="flex justify-center mb-6">
              <img
                src="<%= pokemon.sprites.front_default %>"
                alt="<%= pokemon.name %>"
                class="w-48 h-48 transform transition-transform hover:scale-110"
              />
            </div>
            <div class="text-center">
              <p class="text-xl font-semibold">Height: <%= pokemon.height %></p>
              <p class="text-xl font-semibold">Weight: <%= pokemon.weight %></p>
              <p class="text-xl font-semibold">
                Type: <%= pokemon.types.map(t => t.type.name).join(', ') %>
              </p>
            </div>
          </div>
          <div class="bg-yellow-400 p-4 text-center border-t-4 border-yellow-600">
            <a href="/" class="px-6 py-2 bg-white text-yellow-600 rounded-lg font-semibold transform transition-transform hover:scale-105">Back</a>
          </div>
        </div>
      </div>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>